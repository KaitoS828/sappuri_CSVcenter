services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: supplement-csv-center
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env.local
    environment:
      - NODE_ENV=development
    restart: always
